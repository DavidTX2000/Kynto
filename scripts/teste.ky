// Carregando os módulos dinâmicos
module {./k_hw.so}
module {./k_math.so}
module {./k_ui.so}

print {--- Kynto OS Modular Boot ---}

// Comando do módulo k_hw.so
hw.battery

// Comando do módulo k_math.so
math.calc {50 + 50}

// Comando do módulo k_ui.so
ui.window {Kynto Engine v4.0, 800, 600}

print {--- Teste Concluído ---}
