#include <stdio.h>
#include <string.h>

void calculate(const char* args) {
    // O Kernel chama essa função quando vê 'math.calculate'
    // Se o seu kernel separa 'math.' de 'calculate', o nome aqui é 'calculate'
}
